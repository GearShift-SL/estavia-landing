---

---

<div id="hero" class="max-w-4xl mx-auto px-4 py-16 text-center relative">
  {/* Background Pattern */}
  <div
    class="absolute inset-x-0 bottom-0 rotate-0 h-96 text-gray-500/40 dark:text-gray-300/40 opacity-60 [mask-image:linear-gradient(to_bottom,transparent,white)]"
    style="z-index: -1;"
  >
    <svg class="absolute inset-0 h-full w-full" xmlns="http://www.w3.org/2000/svg">
      <>
        <defs>
          <pattern
            id="grid-pattern"
            width="32"
            height="32"
            patternUnits="userSpaceOnUse"
            x="50%"
            y="100%"
            patternTransform="translate(0 -1)"
          >
            <path d="M0 32V.5H32" fill="none" stroke="currentColor"></path>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid-pattern)"></rect>
      </>
    </svg>
  </div>

  {/* Main Heading */}
  <h1 class="text-5xl font-bold mb-4">
    Gestiona tu inmobiliaria
    <br />
    <span class="text-primary">en piloto automático</span>
  </h1>

  {/* Description */}
  <p class="text-gray-600 text-xl max-w-2xl mx-auto mb-8">
    Automatiza tu agencia inmobiliaria con un CRM que te permite gestionar tus propiedades, clientes, visitas, y
    colaboradores en un solo lugar.
  </p>

  {/* CTA Button */}
  <button
    class="bg-primary text-white px-8 py-3 rounded-full text-lg font-semibold hover:bg-blue-600 transition-colors"
  >
    Solicita una demostración
  </button>

  {/* Space to allow for video overflow */}
  <div class="h-20"></div>
</div>
